_schema-version: 3.3.0
ID: tutorial
description: A simple CAP project.
version: 1.0.0
modules:
- name: tutorial-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: tutorial-db
  - name: tutorial-auth
  - name: tutorial-destination
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    readiness-health-check-http-endpoint: /health
    readiness-health-check-type: http
  build-parameters:
    builder: npm
- name: tutorial-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: tutorial-db
  parameters:
    buildpack: nodejs_buildpack
- name: tutorial-app-deployer
  type: com.sap.application.content
  path: gen
  requires:
  - name: tutorial-html5-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: app/
    requires:
    - artifacts:
      - tutelem.zip
      name: tutorialtutelem
      target-path: app/
    - artifacts:
      - tutui5.zip
      name: tutorialtutui5
      target-path: app/
    - artifacts:
      - tutadmin.zip
      name: tutadmin
      target-path: app//
- name: tutorialtutelem
  type: html5
  path: app/tutelem
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm ci
    - npm run build
    supported-platforms: []
- name: tutorialtutui5
  type: html5
  path: app/tutui5
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm ci
    - npm run build
    supported-platforms: []
- name: tutorial-destinations
  type: com.sap.application.content
  requires:
  - name: tutorial-auth
    parameters:
      service-key:
        name: tutorial-auth-key
  - name: tutorial-html5-repo-host
    parameters:
      service-key:
        name: tutorial-html5-repo-host-key
  - name: srv-api
  - name: tutorial-destination
    parameters:
      content-target: true
  parameters:
    content:
      instance:
        destinations:
        - Name: tutorial-html5-repository
          ServiceInstanceName: tutorial-html5-repo-host
          ServiceKeyName: tutorial-html5-repo-host-key
          sap.cloud.service: tutorial.service
        - Authentication: OAuth2UserTokenExchange
          Name: tutorial-auth
          ServiceInstanceName: tutorial-auth
          ServiceKeyName: tutorial-auth-key
          sap.cloud.service: tutorial.service
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: tutorial
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: tutorial-auth
  - name: tutorial-destination
  - name: tutorial-html5-runtime
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: tutadmin
  type: html5
  path: app/tutadmin
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: tutorial-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: tutorial-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: tutorial-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: tutorial-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: tutorial-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: tutorial-srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
- name: tutorial-html5-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
